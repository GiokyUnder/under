---
import { models } from "../data";
import ModelsPage from "../../../components/ModelsPage.astro";

export async function getStaticPaths({ paginate }) {
  return paginate(models, { pageSize: 6 });
}

const { page } = Astro.props;
---

<ModelsPage
  page={page}
  pageTitle="Элитные эскортницы"
  titleMetaTag="Элитные эскортницы Москвы | Эскорт девушки VIP модели"
  descriptionMetaTag="Лучшие элитные московские эскортницы в Aurora Escort! Анкеты VIP девушек моделей у нас на сайте. Снять девушку на час или ночь, обращайтесь к нам!"
/>
